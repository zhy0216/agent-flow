# AGENTS.md

## 本地技术栈：通过 Herdr 直接修改源码仓库

- 当问题属于本项目使用的本地技术栈或依赖时，通过 Herdr 派发 agent，明确将其工作目录设为对应的本地源码仓库，在那里完成修改和验证。本规则适用于所有本地技术栈，不限于 mad-dom。
- mad-dom 的本地源码仓库是 `/Users/yang/workspace/mad-dom`；需要修改 mad-dom 时，直接派发到该目录。其他本地技术栈先确认实际源码仓库的绝对路径，再派发到对应目录。
- 不要在 agent-flow 中创建 `patches/*.patch` 来代替上游源码修改，也不要把 `.local-deps/sources`、`node_modules` 等生成目录作为修改本地技术栈的交付位置。
- 派发任务时说明问题、验收条件和所需回归验证；遵守目标仓库的 agent 指引，保留已有改动。完成后核对目标仓库的实际 diff 和验证结果，再验证本项目受影响的功能。
- 本地联调使用 `bun run setup:local` 注册修改后的本地依赖，并按脚本提示执行 `bun install`。需要更新可复现依赖时，在上游修复已有可获取的 commit 或发布版本后更新依赖锁定信息。
- 现存补丁需要迁回源码仓库时，先在目标仓库检查并应用、完成验证；待依赖锁定信息改为包含修复的上游版本后，一并移除补丁及其引用，不再新增或扩展补丁作为后续修复方式。
