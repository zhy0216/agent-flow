# Development proxy to the Zebra API. Same-origin /api is used by the browser.
API_PROXY_TARGET=http://127.0.0.1:3001
# For production, reverse-proxy /api to the server. Only set this when that
# API explicitly permits cross-origin browser access; never put secrets here.
# VITE_API_ORIGIN=https://api.example.com
